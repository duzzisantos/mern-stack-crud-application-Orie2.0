export const averageRating = () => {
  // const { length } = rating;
  // const totalRating = rating
  //   .map((element) => element.ratingStars)
  //   .reduce((a, b) => a + b, 0);
  // return totalRating / length;
};
